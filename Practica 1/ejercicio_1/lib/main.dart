import 'package:flutter/material.dart';

import 'src/App.dart';

// Aqui tenemos que la funcion void main como en la mayoria sino es que en todos los lenguajes
//es la aplicacion principal y por ende es lo que ejecuta al inicio el sistema
// en este caso el runApp es la funcion que corre el widget MyApp
void main() {
  runApp(new MyApp());
}


